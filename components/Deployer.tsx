import React from "react";

const Deployer = ({ addr, addrHash }: any) => {
  // console.log('addrHash', addrHash)
  //   console.log('addr', addr)
  return <div className="container">Deployer</div>;
};

export default Deployer;
