import React from "react";

const Sender = ({ addr, addrHash }: any) => {
  // console.log('addrHash', addrHash)
  //   console.log('addr', addr)
  return <div className="container">Sender</div>;
};

export default Sender;
