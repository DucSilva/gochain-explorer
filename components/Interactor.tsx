import React from "react";

const Interactor = ({ addr, addrHash }: any) => {
  // console.log('addrHash', addrHash)
  //   console.log('addr', addr)
  return <div className="container">Interactor</div>;
};

export default Interactor;
